import { cn } from "@/lib/utils";


export default function WorkflowRootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <>
        {children}
           
        </>
    );
}